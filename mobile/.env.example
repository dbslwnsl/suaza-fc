# Expo 는 EXPO_PUBLIC_ 접두사가 붙은 값만 앱 번들에 포함한다.
# 웹(.env.local)의 NEXT_PUBLIC_SUPABASE_* 와 같은 값을 쓴다.
# service_role 키는 절대 여기 넣지 말 것 — 앱 바이너리는 누구나 뜯어볼 수 있다.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
